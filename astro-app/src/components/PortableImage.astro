---
import { urlFor } from "../utils/image";

const { node } = Astro.props;
const hasAsset = !!node?.asset?._ref;
const src = hasAsset ? urlFor(node).url() : undefined;
---
{src ? (
	<img src={src} alt={node?.alt ?? ''} />
) : null}
